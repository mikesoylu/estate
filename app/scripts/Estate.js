var Estate = new Backbone.Marionette.Application();Estate.addRegions({  agency: '#agency',  listings: '#listings'});Estate.on('initialize:after', function() {  Backbone.history.start();});var Listing = Backbone.Model.extend({  defaults: {  }});var Listings = Backbone.Collection.extend({  model: Listing,  initialize: function(listings){ }});var ListingView = Backbone.Marionette.ItemView.extend({  template: "#listing-template",  tagName: 'tr',  className: 'listing',  events: { },  initialize: function(){ }});var ListingsView = Backbone.Marionette.CompositeView.extend({  tagName: "table",  id: "listings",  className: "table-striped table-bordered",  template: "#listings-template",  itemView: ListingView,  initialize: function(){  },  appendHtml: function(collectionView, itemView){    collectionView.$("tbody").append(itemView.el);  }});Estate.addInitializer(function(options){  var listingsView = new ListingsView({    collection: options.listings  });  Estate.listings.show(listingsView);});